# Specify files that shouldn't be modified by Fern
README.md

# Custom wrapper code (polling, webhooks, typed schemas)
src/wrapper/

# Custom index.ts exports the wrapper client
src/index.ts

# Jest config with wrapper tests
jest.config.mjs

# Documentation
docs/

# Prevent Fern from adding unnecessary deps (webpack, ts-loader)
tests/unit/fetcher/stream-wrappers/webpack.test.ts

# Publish workflow — customized to publish v0.x with --tag v0 instead of latest
# Keep ci.yml listed so Fern doesn't regenerate it after the rename
.github/workflows/ci.yml
.github/workflows/publish.yml
