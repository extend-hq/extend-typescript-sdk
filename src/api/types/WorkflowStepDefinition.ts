// This file was auto-generated by Fern from our API Definition.

import type * as Extend from "../index";

/**
 * A workflow step definition used when creating, updating, deploying, and retrieving workflow versions.
 *
 * See the [Configuring Workflows via API guide](https://docs.extend.ai/2026-02-09/product/workflows/configuring-workflows-via-api) for routing patterns and examples.
 */
export type WorkflowStepDefinition =
    | Extend.WorkflowStepDefinition.Trigger
    | Extend.WorkflowStepDefinition.Parse
    | Extend.WorkflowStepDefinition.Extract
    | Extend.WorkflowStepDefinition.Classify
    | Extend.WorkflowStepDefinition.Split
    | Extend.WorkflowStepDefinition.MergeExtract
    | Extend.WorkflowStepDefinition.Conditional
    | Extend.WorkflowStepDefinition.ConditionalExtract
    | Extend.WorkflowStepDefinition.ExternalDataValidation
    | Extend.WorkflowStepDefinition.WebhookResponse
    | Extend.WorkflowStepDefinition.RuleValidation
    | Extend.WorkflowStepDefinition.HumanReview
    | Extend.WorkflowStepDefinition.Collect
    | Extend.WorkflowStepDefinition.FileConversion;

export namespace WorkflowStepDefinition {
    export interface Trigger extends Extend.TriggerStepDefinition {
        type: "TRIGGER";
    }

    export interface Parse extends Extend.ParseStepDefinition {
        type: "PARSE";
    }

    export interface Extract extends Extend.ExtractStepDefinition {
        type: "EXTRACT";
    }

    export interface Classify extends Extend.ClassifyStepDefinition {
        type: "CLASSIFY";
    }

    export interface Split extends Extend.SplitStepDefinition {
        type: "SPLIT";
    }

    export interface MergeExtract extends Extend.MergeExtractStepDefinition {
        type: "MERGE_EXTRACT";
    }

    export interface Conditional extends Extend.ConditionalStepDefinition {
        type: "CONDITIONAL";
    }

    export interface ConditionalExtract extends Extend.ConditionalExtractStepDefinition {
        type: "CONDITIONAL_EXTRACT";
    }

    export interface ExternalDataValidation extends Extend.ExternalDataValidationStepDefinition {
        type: "EXTERNAL_DATA_VALIDATION";
    }

    export interface WebhookResponse extends Extend.WebhookResponseStepDefinition {
        type: "WEBHOOK_RESPONSE";
    }

    export interface RuleValidation extends Extend.RuleValidationStepDefinition {
        type: "RULE_VALIDATION";
    }

    export interface HumanReview extends Extend.HumanReviewStepDefinition {
        type: "HUMAN_REVIEW";
    }

    export interface Collect extends Extend.CollectStepDefinition {
        type: "COLLECT";
    }

    export interface FileConversion extends Extend.FileConversionStepDefinition {
        type: "FILE_CONVERSION";
    }
}
