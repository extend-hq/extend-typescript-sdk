// This file was auto-generated by Fern from our API Definition.

import type * as Extend from "../index";

/**
 * The extractor, classifier, or splitter that was run.
 *
 * **Availability:** Present when an entity was provided when creating the batch run.
 */
export type BatchRunEntity =
    | Extend.BatchRunEntity.Extractor
    | Extend.BatchRunEntity.Classifier
    | Extend.BatchRunEntity.Splitter;

export namespace BatchRunEntity {
    export interface Extractor extends Extend.ExtractorSummary {
        object: "extractor";
    }

    export interface Classifier extends Extend.ClassifierSummary {
        object: "classifier";
    }

    export interface Splitter extends Extend.SplitterSummary {
        object: "splitter";
    }
}
