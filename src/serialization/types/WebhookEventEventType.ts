/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as serializers from "../index";
import * as Extend from "../../api/index";
import * as core from "../../core";

export const WebhookEventEventType: core.serialization.Schema<
    serializers.WebhookEventEventType.Raw,
    Extend.WebhookEventEventType
> = core.serialization.enum_([
    "workflow_run.completed",
    "workflow_run.failed",
    "workflow_run.needs_review",
    "workflow_run.rejected",
    "workflow_run.step_run.processed",
    "processor_run.processed",
    "processor_run.failed",
    "workflow.created",
    "workflow.deployed",
    "workflow.deleted",
    "processor.created",
    "processor.updated",
    "processor.deleted",
    "processor.draft.updated",
    "processor.version.published",
]);

export declare namespace WebhookEventEventType {
    export type Raw =
        | "workflow_run.completed"
        | "workflow_run.failed"
        | "workflow_run.needs_review"
        | "workflow_run.rejected"
        | "workflow_run.step_run.processed"
        | "processor_run.processed"
        | "processor_run.failed"
        | "workflow.created"
        | "workflow.deployed"
        | "workflow.deleted"
        | "processor.created"
        | "processor.updated"
        | "processor.deleted"
        | "processor.draft.updated"
        | "processor.version.published";
}
